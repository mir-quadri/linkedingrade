# ECC rules (vendored)

Source: https://github.com/affaan-m/ECC `rules/{common,typescript,react,web}` @ bf70150.
Loaded for every agent session in this repo alongside the `ecc@ecc` plugin (`.claude/settings.json`).

**Precedence:** this repo's own `AGENTS.md` / `CLAUDE.md` is the source of truth. Where these rules disagree with it, the repo's file wins.

Update: re-copy the folders from a newer ECC commit and bump the SHA above.
